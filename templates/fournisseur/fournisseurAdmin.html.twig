{% extends 'base1.html.twig' %}
{% block menu%}
    <!-- Sidebar Start -->
    <div class="sidebar pe-4 pb-3">
        <nav class="navbar bg-light navbar-light">
            <a href="index.html" class="navbar-brand mx-4 mb-3">
                <h3 class="text-primary"><i class="fa fa-hashtag me-2"></i>Mall For All</h3>
            </a>

            <div class="navbar-nav w-100">
                <a href="index.html" class="nav-item nav-link active"><i class="fa fa-tachometer-alt me-2"></i> Admin Dashboard </a>
                <a href="#" class="nav-item nav-link" ><i class="fa fa-laptop me-2"></i>Users</a>
                <a href="#" class="nav-item nav-link"><i class="fa fa-th me-2"></i>Shops</a>
                <a href="#" class="nav-item nav-link"><i class="fa fa-shopping-bag me-2"></i>Products</a>
                <a href="#" class="nav-item nav-link"><i class="fa fa-shopping-cart me-2"></i>Commandes</a>
                <a href="{{path('afficheF')}}" class="nav-item nav-link"><i class="fa fa-chart-bar me-2"></i>Suppliers</a>
                <a href="{{path('afficheCF')}}" class="nav-item nav-link"><i class="fa fa-chart-bar me-2"></i>Supplier's categories</a>
                <a href="#" class="nav-link nav-link" ><i class="far fa-calendar me-2"></i>Events</a>
                <a href="#" class="nav-link nav-link" ><i class="far fa-file-alt me-2"></i>Reclamations</a>

            </div>
        </nav>
    </div>
    <!-- Sidebar End -->
{% endblock %}
{% block table%}
    <div class="container-fluid pt-4 px-4">
        <div class="bg-light text-center rounded p-4">
            <div class="d-flex align-items-center justify-content-between mb-4">
                <h6 class="mb-0">List of suppliers</h6>
                <a href="{{ path('addF') }}">Add new supplier</a>
            </div>
            <div class="table-responsive">
                <table class="table text-center align-middle table-bordered table-hover mb-0">
                    <thead>
                    <tr class="text-dark">
                        <th scope="col"></th>
                        <th scope="col"> id </th>
                        <th scope="col"> Name </th>
                        <th scope="col"> Email </th>
                        <th scope="col"> Phone number </th>
                        <th scope="col"> Category </th>
                        <th scope="col"> Actions </th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for i in fournisseurs %}
                        <tr>
                            <th scope="col"><input class="form-check-input" type="checkbox"></th>
                            <td> {{ i.id }}</td>
                            <td>{{ i.nom}} </td>
                            <td>{{ i.email}} </td>
                            <td>{{ i.tel}} </td>
                            <td>{{ i.categorie.libelle}} </td>
                            <td>
                                <a class="btn btn-sm btn-primary" href="{{ path('suppF', { 'id':i.id }) }}">Delete</a>
                                <a class="btn btn-sm btn-primary" href="{{ path('updateF', { 'id':i.id }) }}">Update</a>
                                <a class="btn btn-sm btn-primary" href="{{ path('detailF', { 'id': i.id  }) }}">Details</a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}

